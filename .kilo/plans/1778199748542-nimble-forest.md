# Plan: Remove Speed Functionality and Reduce Audio Player Card Size

## Goal
1. Remove playback speed control functionality from AudioPlayerCard component
2. Reduce the audio player card size to half of its current dimensions

## Changes Required

### 1. Remove Speed Functionality (src/components/Audio/AudioPlayerCard.tsx)
- Remove speed state: `[speed, setSpeed] = useState(1)` (line 154-155)
- Remove speed effect: `useEffect(() => { if (audioRef.current) { audioRef.current.playbackRate = speed; } }, [speed])` (lines 327-331)
- Remove speed handler: `handleSpeedChange` function (lines 394-399)
- Remove speed options memo: `const speedOptions = useMemo(() => [0.75, 1, 1.25, 1.5] as const, []);` (line 183)
- Remove speed control UI:
  - Speed toggle button (lines 733-746)
  - Speed menu popup (lines 749-769)
- Remove speed-related props and TypeScript definitions if any (none found)

### 2. Reduce Card Size by Half
- Current container max-width: `max-w-[300px]` (line 501)
- Change to: `max-w-[150px]` (half of 300px)
- Consider adjusting padding/margins proportionally if needed
- Height will adjust automatically based on content

## Implementation Notes
- Ensure audio playback still works normally at 1x speed after removal
- Test that removing speed controls doesn't break layout
- Verify all other controls (play/pause, volume, navigation, etc.) remain functional
- Check responsive behavior at different screen sizes

## Files to Modify
- src/components/Audio/AudioPlayerCard.tsx

## Testing Plan
- Verify audio plays at normal speed
- Confirm speed controls are completely removed
- Check card width is approximately half original
- Ensure all other functionality remains intact